# LAX OSINT — Backend
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-dotenv==1.0.1
httpx>=0.26,<0.28   # متوافق مع supabase 2.9 (يتطلب httpx<0.28)
pydantic==2.10.4
email-validator==2.2.0
supabase==2.9.0
bcrypt==4.2.1

# أدوات البحث (مثبتة تلقائيًا في الصورة)
maigret==0.6.5              # اليوزر نيم (3000+ موقع) — أحدث إصدار متوفر
holehe==1.61                # الإيميل (120+ موقع)
duckduckgo-search>=6.0.0    # الاسم الكامل (مع تناوب UA ومحاولة إعادة)
googlesearch-python>=1.2.0  # محرك احتياطي للاسم الكامل إن حُجب DuckDuckGo
beautifulsoup4>=4.14.3,<5   # مطلوب من socid-extractor (تابع maigret)

# PhoneInfoga يُنزَّل كـ binary رسمي في Dockerfile (غير متوفر على PyPI):
#   github.com/sundowndev/phoneinfoga/releases (v2.11.0 Linux x86_64)

# التحليل الشامل للهاتف (تلقائي-مستقل) + استدعاءات OpenRouter
phonenumbers>=9.0,<10.0   # تحليل تقني كامل + علم الدولة (بدون اتصال خارجي)
requests>=2.31,<3.0       # OpenRouter chat + RDAP WHOIS
dnspython>=2.6.0          # كشف مزوّد الإيميل عبر سجلات MX (Gmail/Outlook/Yahoo…)
PySocks>=1.7.1            # وكيل Tor socks5h للبحث العميق في الديب ويب (عند تكوين TOR_PROXY)

# أدوات إضافية اختيارية (تُفك تعليقها عند الحاجة فقط)
# shodan>=1.29.0          # Shodan API (في حالة مفتاح SHODAN_API_KEY)
# censys>=2.2.0           # Censys (في حالة CENSYS_API_ID/SECRET)
# telethon>=1.36.0        # فحص Telegram برقم عبر جلسة (تتطلب TELEGRAM_* في env)
# playwright>=1.49.1
# facebook-scraper>=0.3.0